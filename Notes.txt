Rules
#1. Constants are public, final & static
#2. 


Local variable - variable within a method, once the method is finished the variable dissapears
Instance Variable - 
Class Variable - a static variable 

project 1 - reorder the csv file alphabetically

List vs Set - List is in order, Set is not, set cannot have duplicates, a list can

classes for project 2
garage class - main
kiosk
Ticket

input and output in own classes
validate strings(length and null)
composition over inheritence